{{!< default}}
<header id="header" class="header" {{#if @blog.cover}}" style="background-size: cover; background-position: center; background-image: url({{@blog.cover}}){{else}}no-cover{{/if}}">
    <div class="container">
        <h1 class="title">{{@blog.title}}</h1>	
        <div class="desc">
        {{@blog.description}}
        </div>
        {{navigation}}
        <a href="#" class="menu" id="toggle">
            <span></span>
            <span></span>
            <span></span>
            <span></span>
        </a>
    </div>
</header>

<div class="container">
    <div class="content">
    {{! The tag below includes the post loop - partials/loop.hbs }}
        {{> "loop"}}
    </div> <!-- #main -->  
</div>